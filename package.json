{
  "name": "@pragmaproducts/pragma-input",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "Johan Rabie",
  "license": "ISC",
  "devDependencies": {
    "aurelia-polyfills": "^1.1.0",
    "aurelia-testing": "^1.0.0-beta.2.0.0",
    "build-utilities": "0.0.88"
  },
  "jspm": {
    "dependencies": {
      "aurelia-bootstrapper": "npm:aurelia-bootstrapper@^1.0.0",
      "aurelia-framework": "npm:aurelia-framework@^1.0.1"
    },
    "devDependencies": {}
  },
  "dependencies": {
    "typings": "^1.3.3"
  },
  "scripts": {
    "audit": "gulp clean:coverage && gulp audit:all && gulp test:coverage && gulp build:all && gulp test:e2e && gulp test:generate-report",
    "test": "gulp test",
    "test:ui": "gulp build:all && gulp test:e2e",
    "build": "gulp build:all",
    "watch:ui": "gulp build:all && gulp watch:ui",
    "watch:all": "gulp build:all && gulp watch:all",
    "run": "gulp build:all && gulp serve"
  }
}
